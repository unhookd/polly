name: primary

on: [push]

jobs:

  build:

    runs-on: ubuntu-20.04

    container:
      image: ubuntu:focal-20200606
      options: "--user 0"

    steps:
    - uses: actions/checkout@v1
    - name: bootstrap
      run: |
        set -e
        bash bin/github-actions.sh
